source "https://rubygems.org"

# The exact stack GitHub Pages runs, so a local preview matches the live build.
# Only needed if you want to preview before pushing - GitHub builds it either way.
gem "github-pages", group: :jekyll_plugins
